# Agent 项目规则

开始任务时默认只读取 `HANDOFF.md`；仅在当前问题需要时读取 `DECISIONS.md`、`LESSONS.md` 或日期记忆。压缩、交接或阶段结束前执行 `PRE_COMPACTION_CHECKLIST.md`。`PreCompact` 只做确定性检查并保留证据入口；压缩后根据 `recovery_required` 决定是否定向回查 transcript。项目专属规则继续写在本文件中。

当用户确认关键决策、约束、验收标准、已验证经验、当前阻塞或恢复所需状态时，当轮写入对应文件并回读验证，不要等到压缩。用户明确说“记住这条”或同义表达时必须立即持久化并告知路径。

`HANDOFF.md` 只能有一个当前目标和一个下一步，最近里程碑最多 3 条，目标 500～1000 字，硬上限 1500 字；超限时重写收缩。

当前目标未完成时，先判断新输入是否推进当前目标，再判断持续时间。与当前目标直接相关的追问、质疑、纠错、同一交付物新增要求、实现调整和结果检查留在当前任务；明显不推进当前目标且可能开启独立讨论或行动方向的输入，即使可能只持续一两轮，也轻量提醒一次是否新建 Codex 任务，不要求用户声明正在测试。用户选择留在当前任务后，同一边界不重复提醒；只有新的独立边界才再次提醒。未经明确请求不要代替用户创建任务；当前目标已完成后，不因下一个话题不同而提醒。

# Agent 记忆工作流

## 分层

- 原始会话：事实档案，用于追溯和重新提取。
- `HANDOFF.md`：当前任务状态和单一下一步。
- `DECISIONS.md`：已确认的重要决策、原因和失效条件。
- `LESSONS.md`：经过真实任务验证的可复用经验。
- `memory/YYYY-MM-DD.md`：情节记录和待审核候选。

## 写入规则

1. 摘要不是事实源；重要结论必须带来源或验证方式。
2. 用户确认的关键决策、约束、验收标准、已验证经验、阻塞和恢复状态在产生时立即落盘并回读验证，不等待压缩。
3. 未确认推断只能进入日期记忆，不得直接晋升。
4. 只有用户确认、真实验证或重复证明有价值的信息才能进入长期文件。
5. 信息冲突时保留历史并标记取代关系，不静默覆盖。
6. 四层文件全部创建，但任务开始默认只读 `HANDOFF.md`，其余按需读取。
7. 不保存密钥、令牌或其他敏感凭据。

## `HANDOFF.md` 预算

- 一个当前目标、一个下一步。
- 当前阻塞只保留仍有效项。
- 最近里程碑最多 3 条。
- 关键文件只保留恢复所需入口。
- 目标 500～1000 字，硬上限 1500 字；超限必须重写收缩。
